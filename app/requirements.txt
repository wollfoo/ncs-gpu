retrying
GPUtil
scikit-learn
flask
celery
requests
pyelftools
loguru
psutil
tensorflow
joblib
numpy
cryptography

# ===== PYTORCH REMOVAL - NOT NEEDED FOR MINING =====
# Analysis: Codebase does not use PyTorch anywhere
# Mining uses pure CUDA via inference-cuda binary, not Python ML libraries
# Removing PyTorch prevents CUDA version conflicts entirely

aiofiles
aiorwlock
pynvml==11.4.1
readerwriterlock
ratelimiter
openai
pandas
pydantic
grpcio 
grpcio-tools
dependency-injector
uvicorn
aiohttp
pycryptodome
azure-identity
azure-mgmt-resource
azure-mgmt-resourcegraph
azure-mgmt-network
azure-mgmt-security
azure-loganalytics
azure-mgmt-loganalytics
azure-monitor-query 
azure-mgmt-machinelearningservices
azure-ai-ml
azure-ai-anomalydetector
pyyaml>=6.0,<7.0
prometheus-client>=0.16.0,<0.17.0

# Thư viện Python cho xử lý hệ thống
importlib-metadata>=4.12.0
types-dataclasses>=0.6.6
types-requests>=2.28.11.17
typing-extensions>=4.5.0

# Thư viện cho compile và xử lý code
types-regex>=2022.10.31.3

# libbpf-python sẽ được cài qua apt
pyroute2

# EventBus dependencies
jsonschema>=4.17.0  # JSON Schema validation

# Phase 2-4: Redis backend dependencies
redis>=4.5.0
hiredis>=2.0.0  # Performance backend for redis-py

# Phase 4-5: RabbitMQ backend dependencies (DISABLED - using memory backend)
# pika>=1.3.0  # RabbitMQ client library
# kombu>=5.3.0  # High-level messaging library

# Phase 5-6: Kafka backend dependencies (sẽ thêm khi implement)
# kafka-python>=2.0.0
# confluent-kafka>=2.0.0

# Development dependencies
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=6.0.0