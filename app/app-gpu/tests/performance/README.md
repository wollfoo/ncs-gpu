# Performance Harness (TODO)

- Nsight Systems cho GPU profiling.
- k6 cho API load test.
- Script `tooling/scripts/bench_scheduler.sh` gửi N yêu cầu song song để kiểm tra throughput scheduler ↔ NATS (sử dụng `SCHEDULER_BENCH_TOKEN` nếu cần Auth).
