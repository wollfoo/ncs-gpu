version: "3.9"
services:
  core:
    environment:
      - APP_CONFIG_PATH=/opt/app/configs/test.yaml
    volumes:
      - ../fixtures/test.yaml:/opt/app/configs/test.yaml:ro
  controller:
    environment:
      - CORE_ENDPOINT=http://core:8080
