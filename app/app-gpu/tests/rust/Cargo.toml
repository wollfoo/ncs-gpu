[package]
name = "tests-rust"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = { version = "0.3", default-features = false }
tokio-test = "0.4"
app-gpu-scheduler = { path = "../../internal/scheduler" }
app-gpu-domain = { path = "../../internal/domain" }

[[bench]]
name = "scheduler"
harness = false
